{
  "name": "youtube-time-limiter",
  "version": "0.9.0",
  "description": "Local-first YouTube viewing budgets with a live timer and configurable limit actions.",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test tests/unit/*.test.cjs tests/integration/*.test.cjs",
    "check": "node scripts/check.cjs && npm test",
    "prototype": "node scripts/prototype-server.cjs",
    "test:unit": "node --test tests/unit/*.test.cjs",
    "test:integration": "node --test tests/integration/*.test.cjs"
  }
}
